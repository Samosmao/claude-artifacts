# URL of your deployed Cloudflare Worker (from `wrangler deploy` output)
# For local dev with `wrangler dev`, this defaults to http://127.0.0.1:8787/api/chat
VITE_WORKER_URL=https://your-worker-name.your-subdomain.workers.dev/api/chat
